为图床管理工具创建一个 Vue3 组件表单，支持上传图片及填写标签和描述，要求如下：

- 使用 `<script setup>` 格式；
- 使用 Composition API；
- 使用 Vuetify 组件库；
- 表单字段包括：
  - 上传图片（拖拽或选择）；
  - 标签（支持多个，用逗号分隔或 chip）；
  - 描述备注（可选）；
- 提交后以 JSON 结构打印或发送 POST 请求；
- 界面清爽，样式统一；
- 添加简要注释。

输出应为完整组件代码。
